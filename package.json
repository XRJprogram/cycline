{
  "name": "cycline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node build_standalone.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "lamejs": "^1.2.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
